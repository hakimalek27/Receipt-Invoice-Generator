# Deep Audit of the Multi-Company Document Generator Plan

## Bottom-line verdict

**A. Verdict:** the plan is **conditionally acceptable as an audit-output handoff**, but **not yet acceptable as an implementation baseline**. The adjusted crosscheck preserves the main locked decisions: Excel references are not production imports, design stays “match + kemas,” company data and numbering stay isolated, official numbers are issued only at issue time, e-Invoice stays metadata-ready only, the workspace is still a new build, and this pass is planning rather than app code. That said, there are still several blockers that should be resolved before scaffolding starts, especially around canonical template isolation, numbering-format freeze, receipt taxonomy, issue-time data snapshots, and security controls for Telegram, AI, PDF rendering, and attachments. fileciteturn0file0 fileciteturn0file1 fileciteturn0file2

In short: **accept the plan for revision work, reject it for coding until Phase 0.5 is tightened and completed**. That is also consistent with the crosscheck’s own recommendation to insert a missing-info and scope-freeze gate before build work begins. fileciteturn0file1

## Comparison of the original plan, the Claude audit-output plan, and the Codex crosscheck verdict

The **original Codex plan** is already strong as a product-and-architecture baseline. It covers the new-workspace assumption, multi-company isolation, the draft-to-issue lifecycle, A4 PDF plus 60 mm thermal output, artwork attachments, REST API support, Telegram-assisted drafting, optional DeepSeek parsing, e-Invoice metadata readiness, audit trails, and a single-host Tencent Ubuntu deployment model. Its biggest weakness is not strategy, but **precision**: some controls were still too high-level, some workflow semantics were ambiguous, and some implementation-critical data structures were implied rather than crisply defined. fileciteturn0file1 fileciteturn0file2

The **Claude audit-output plan**, as summarized in the crosscheck JSON, materially improves reviewability. It adds a sibling output folder, an executive summary, a formal risk register, a grouped missing-info checklist, clearer phase ordering, and a dedicated schema/API changes document. Those are real improvements because they convert a large planning JSON into a review pack that another human or coding agent can follow with less ambiguity. fileciteturn0file1

The **Codex crosscheck verdict** is the right overall judgment: accept Claude’s structure, but only after tightening several items. The most important accepted improvements are the risk register, Phase 0.5 scope freeze, issue-path idempotency, line-level e-Invoice readiness, and stronger deployment hardening. The most important required adjustments are also correct: keep schema ideas explicitly “proposed” rather than final migrations, do not let revisions weaken issued-document immutability, scope PDF deduplication by company/document/template context, keep MyInvois references current and re-verifiable, and do not let future document types expand v1 scope by accident. fileciteturn0file1

My conclusion after comparing all three layers is this: **the original plan is strategically sound, the Claude structure is a better delivery shape, and the crosscheck is directionally correct — but the revised output still needs a sharper operational contract before anyone starts coding**. fileciteturn0file1 fileciteturn0file2

## Blockers and issues by severity

**B. Severity-ranked issues:**

**Critical**

| Issue | Why it is a blocker | Specific correction |
|---|---|---|
| Canonical template isolation is still too easy to misunderstand | The reference pack is heavily mixed. `PERSADA GEMILANG GLOBAL.xlsx` contains Persada `INV 1`, but also a Virtue sheet, a Wehdah sheet, and multiple A To Z generic sheets. `Projek Masjid Muaz Bin jabal (2).xlsx` mixes NAS Ceria quotations, NAS invoices, hidden legacy sheets, costing/inventory sheets, and a Virtue Damsel partial sheet. Wehdah workbooks also mix real Wehdah sheets with A To Z generic templates. Without an explicit allowlist, template/company/numbering leakage is the biggest failure mode in the whole project. fileciteturn0file1 fileciteturn0file4 fileciteturn0file5 | Add a **canonical_source_registry** to the revised JSON/markdown: `company -> document_type -> allowed reference files/sheets/assets`. Everything else should be explicitly marked `non_canonical_reference_only` or `deny_for_seeding`. |
| Numbering design is not frozen tightly enough | The historical references show inconsistent numbering realities: NAS Ceria uses bare numeric examples such as `3118`, `2291`, `1292`, and `3893`; Wehdah uses mixed `I-`, `INV-`, and `Q-` patterns; Persada `INV 1` shows `202601030025`. Meanwhile, the plan also gives generic example prefixes such as `WS-INV-26001`, `PGG-INV-26001`, and `NCS-Q-26001`, and open questions still ask for final prefixes for Wehdah and Nas Ceria. That is a real risk because implementers may mistake examples for approved patterns. fileciteturn0file1 fileciteturn0file2 fileciteturn0file3 fileciteturn0file5 | Make numbering a **hard Phase 0.5 freeze item for every company**, including Persada. Add a configurable numbering-policy model per `company_id + document_type`, with pattern tokens, prefix/suffix, year format, padding, reset policy, and a non-binding example preview. |
| Receipt taxonomy is ambiguous | The current plan uses `receipt` as the formal **Official Receipt** document type, while also defining a 60 mm thermal “receipt/quick print” output. In daily implementation, that ambiguity will cause wrong naming in API routes, wrong permissions, and possibly the wrong numbering or status model. In Malaysia SME usage, a formal official receipt and a thermal quick-print slip are not always the same operational artifact. fileciteturn0file2 | Rename the internal document type to **`official_receipt`**, and treat thermal output as either a print layout for official receipts or a separate `quick_print_receipt` artifact. Do not overload `receipt` to mean two different things. |

**High**

| Issue | Why it matters | Specific correction |
|---|---|---|
| Issued-document immutability is strong at PDF level, but still under-specified at structured-data level | The plan clearly protects immutable PDFs and template versions, but the revised baseline should also say that supplier profile, buyer profile, bank details, terms, tax metadata, and template identity are **snapshotted on issue**. Otherwise future profile edits can change API views, exports, or later e-Invoice preparation even if the PDF is immutable. fileciteturn0file1 fileciteturn0file2 | Add `issuer_snapshot_json`, `buyer_snapshot_json`, `bank_snapshot_json`, `terms_snapshot_json`, `template_version_id`, and `issue_timezone_snapshot` to the issue-time model. |
| The `number preview` endpoint comes too close to violating the “number only on issue” lock | The plan lists `number preview` as a core API endpoint, while also locking official numbering to issue time. If that preview returns the actual next sequence, users will treat it as a reservation even if the system does not. fileciteturn0file1 fileciteturn0file2 | Either remove the endpoint from v1, or explicitly redefine it as a **format example only** with no reservation semantics. |
| Payment and official-receipt allocation need to be first-class, not incidental | The original plan mentions partial/full payment and receipt generation, while the crosscheck correctly adds `payment_allocations`. That addition should become mandatory, because one payment may settle multiple invoices and one invoice may receive multiple payments. Without that model, official receipts and payment status become unreliable. fileciteturn0file1 fileciteturn0file2 | Promote `payment_allocations` from a recommendation into a required design element in the revised plan. |
| Telegram issue flow still needs stronger replay and authorization wording | The plan correctly says Telegram cannot issue without explicit confirmation, but it should go further: confirmation must be bound to `draft_hash`, `company_id`, `user_id`, `chat_id`, and an expiry time, with idempotency and webhook-secret verification. Otherwise retries and chat spoofing become real risks. fileciteturn0file1 fileciteturn0file2 | Add an explicit **confirmation-token contract** and test cases for replay, expiry, unauthorized chat, and cross-company access. |
| PDF rendering and attachment handling need a stricter security boundary | The plan already mentions private storage, signed URLs, MIME checks, and the risk register includes path traversal and PDF leakage. That is good, but not enough for HTML-to-PDF rendering with user-supplied text and attachments. The system should explicitly forbid uncontrolled remote fetches, arbitrary local file paths, and unsafe embedded assets. fileciteturn0file1 fileciteturn0file2 | Add controls for render-time network restrictions, asset allowlists, SVG sanitization or rasterization, path normalization, and storage authorization checks. |
| DeepSeek controls are directionally right but still too light for production governance | The AI role is correctly limited to parsing/drafting and never treated as source of truth, but the plan should also define retention, redaction policy, provider failure behavior, malformed JSON handling, and prompt-injection containment more explicitly. fileciteturn0file1 fileciteturn0file2 | Add an AI governance block with retention limits, queue timeout/fallback behavior, schema validation, and an explicit rule that AI never chooses company, never chooses final number, and never bypasses confirmation. |

**Medium**

| Issue | Why it matters | Specific correction |
|---|---|---|
| The extracted workbook metadata is useful, but some fields are heuristic and not authoritative | In the workbook summary, generic A To Z sheets often show `Date:` as detected document number, and some NAS sheets show `Item | Description` as the detected document number. That is fine for exploratory audit, but dangerous if a later agent mistakes the extraction as production truth. fileciteturn0file5 | Add a plain warning to the revised plan: `detected_*` workbook fields are heuristic audit extraction only, never canonical master data. |
| Wehdah artwork placement should be template-controlled, not freely user-positioned by default | The audit plan allows artwork pages before or after the main document, but the strongest real reference — especially `INV-SIGNAGE dec.pdf` — shows an invoice page followed by artwork confirmation pages. A free toggle risks workflow drift. fileciteturn0file2 fileciteturn0file4 fileciteturn0file5 | Make artwork ordering a **template-family rule**. Default Wehdah to `main invoice first, artwork after`, and only support alternative ordering if a real business case appears later. |
| Acceptance criteria are still too broad | Current acceptance criteria verify pack integrity and non-mixing at a high level, but do not yet function as true implementation gates for concurrency, numbering, PDF immutability, signed URL expiry, AI rejection cases, or backup restore. fileciteturn0file1 fileciteturn0file2 | Expand acceptance criteria into measurable checks, not just document-presence checks. |
| Persada identity assets are workable, but still not production-final | The Persada workbook and the letterhead use slightly different address wording, the logo is a white-background JPG, and open questions still leave room for a better vector/transparent logo and confirmation of website usage on billing PDFs. fileciteturn0file3 fileciteturn0file4 fileciteturn0file5 | Freeze canonical Persada billing identity before pixel-level template work starts. |
| Thermal receipt requirements are still shallow | The plan correctly says 60 mm, high-contrast, thermal-friendly, and optionally future ESC/POS, but it should also define printer-safe content width, maximum characters per line, QR constraints, Unicode tests, and margin behavior. fileciteturn0file2 | Add thermal-print acceptance tests and a line-width specification. |

**Low**

| Issue | Why it matters | Specific correction |
|---|---|---|
| Future MyInvois-related document types are mentioned in a way that can confuse scope | `refund_note` and self-billed variants are mentioned in readiness planning. The crosscheck already warns against promoting them into v1 UI scope, but that warning should be even more explicit in the revised output. fileciteturn0file1 fileciteturn0file2 | Keep them in a `future_only` block, not in v1 workflow tables. |
| Some execution-hygiene items belong to delivery process, not product scope | INDEX link checks and git-diff checks are useful, but they should not distract from product audit findings. fileciteturn0file1 | Keep them in a “handoff QA” appendix, not in the main product plan. |

## File-by-file audit of the Excel, PDF, and image references

For **NAS Ceria**, `Q-3118.xlsx` is a valid canonical style reference for the Vertex42-derived quote/invoice family: mostly Arial, portrait layout, the familiar `No / Item-Description / Qty / Rate / Amount` structure, and classic quotation/invoice blocks. However, its `Modal` sheet is not a billing template at all; it is a costing/purchase list with receipt references. More importantly, the workbook still contains spreadsheet artifacts that should never be copied directly into production, such as quote-template remnants on invoice layouts and helper text from the old Excel template family. This is exactly where “match + kemas” matters: preserve the identity, but remove spreadsheet junk. fileciteturn0file1 fileciteturn0file4 fileciteturn0file5

`Projek Masjid Muaz Bin jabal (2).xlsx` is **not** a clean template source. It is a mixed workbook with visible and hidden NAS Ceria quotations, NAS Ceria invoices, hidden legacy sheets, project cost/inventory sheets, old quote examples, and a Virtue Damsel partial sheet. That makes it valuable as a reference pack, but dangerous as a source of truth. It should only be mined after sheet-by-sheet classification, and the final plan should say that explicitly. fileciteturn0file1 fileciteturn0file5

For **Wehdah**, the references are consistent enough to define a real template family. `INV-ABG HANIF BANNER.xlsx`, `INV-latest.xlsx`, `sham.xlsx`, `QUOTATION MUBZ (1).xlsx`, `QUOTATION MUBZ (2).xlsx`, and `INV-SIGNAGE dec.pdf` all support the same broad conclusions: Lao UI/Rockwell styling, dark blue title areas, structured metadata blocks, item columns with `Item / Description / Qty / Unit / Unit Price / Discount / Total Price`, amount-in-words, bank details, terms, and strong sign/chop positioning. The artwork behavior is also real, not hypothetical: invoice pages and artwork confirmation pages are part of the actual workflow. The main risks are mixed A To Z sheets inside the workbooks, inconsistent historical numbering patterns, and layout fragility if Docker fonts do not match the reference family. fileciteturn0file1 fileciteturn0file4 fileciteturn0file5

`INV-SIGNAGE dec.pdf` is especially important because it is the clearest output target for Wehdah. It shows a two-page structure: page one is the issued invoice with customer details, item table, totals, bank line, terms, and sign/chop areas; page two is an artwork confirmation page with artwork labels and a confirmation/sign-chop section. That PDF should carry more weight than any messy workbook sheet when deciding final behavior for the Wehdah artwork flow. fileciteturn0file4 fileciteturn0file5

For **Persada**, the audit is clear: use **`PERSADA GEMILANG GLOBAL.xlsx` sheet `INV 1` only**, together with the Persada logo, stamp, and letterhead assets. Do **not** treat the rest of the workbook as Persada truth. The workbook summary shows that other sheets include A To Z generic documents, a Virtue Damsel sheet, and a Wehdah invoice sheet. The assets are usable, but still not fully finalized for production: the logo is a white-background JPG, the stamp PNG is transparent and suitable for overlay, and the letterhead is full-page and better suited to formal documents unless the user explicitly wants it on billing PDFs. The canonical address still needs to be reconciled between workbook and letterhead versions. fileciteturn0file3 fileciteturn0file4 fileciteturn0file5

For **Virtue Damsel**, the current evidence only supports a placeholder company capability, not a finalized v1 company rollout. The identity is partial, appears in mixed sheets, and still lacks confirmed active-scope status, final template source, full assets, and tax/bank confirmations. The current plan is right to keep Virtue in the data model but wrong to let any implementer treat it as a ready-made tenant. fileciteturn0file1 fileciteturn0file3 fileciteturn0file4 fileciteturn0file5

For the **A To Z generic templates**, the existing plan is mostly correct: they are useful structural references for Delivery Order, Cash Bill, Purchase Order, Credit Note, Debit Note, Official Receipt, and Payment Voucher, but they are not a real company profile and must never be seeded as a tenant unless the user later confirms that. This rule should be repeated more than once in the revised output because the workbook mix is the single biggest source of confusion in the whole pack. fileciteturn0file1 fileciteturn0file4 fileciteturn0file5

## Specific corrections and additional features

**C. Specific corrections to the current plan**

| Area | Required correction |
|---|---|
| Canonical sources | Add a machine-readable allowlist for `company_id + document_type + canonical_reference_source`. Explicitly mark all mixed sheets as non-canonical. fileciteturn0file1 fileciteturn0file5 |
| Numbering | Add a `numbering_policies` concept with configurable prefix, suffix, separator, sequence padding, year token, reset policy, scope dimensions, and non-binding preview example. Make Persada numbering confirmation a missing-info item too. fileciteturn0file1 fileciteturn0file2 fileciteturn0file3 |
| Receipt model | Rename internal `receipt` to `official_receipt`, and separate legal receipt lifecycle from thermal/slip output. fileciteturn0file2 |
| Issue-time snapshots | Explicitly snapshot issuer, buyer, bank, terms, tax, currency/FX, and template identity on issue. fileciteturn0file1 fileciteturn0file2 |
| API | Keep `POST /documents/{id}/issue` idempotent; make `number preview` non-reserving or remove it; add immutable PDF version retrieval and attachment validation rules. fileciteturn0file1 fileciteturn0file2 |
| Payments | Make `payment_allocations` mandatory so official receipts, partial payments, and invoice balances stay correct. fileciteturn0file1 fileciteturn0file2 |
| Telegram | Require confirmation tokens bound to draft hash, user, company, chat, expiry, and idempotency. fileciteturn0file1 fileciteturn0file2 |
| PDF and attachments | Add render sandbox rules, asset allowlists, MIME sniffing, path normalization, and short-lived signed URLs. fileciteturn0file1 fileciteturn0file2 |
| Tests | Expand acceptance gates to include concurrency, year rollover in Asia/Kuala_Lumpur, unauthorized Telegram access, visual regression, attachment traversal rejection, and backup restore. fileciteturn0file1 fileciteturn0file2 |

**D. Additional features that are genuinely relevant**

The best feature additions are the ones that reduce future rework without turning the system into a full ERP. For **Malaysia SME use**, I would add company-configurable bank-account selection, bilingual label support, project/site/PO/reference fields, partial-payment plus official-receipt allocation, and optional DuitNow/QR support as a template asset rather than hard-coded logic. For **global use**, the key additions are multi-currency issue snapshots, tax-ID variants by country, localized date/number formats, and clear zero-rated/exempt tax handling at line level. These additions are aligned with the current plan’s global ambitions and e-Invoice-readiness direction without violating the v1 scope lock. fileciteturn0file1 fileciteturn0file2

The missing-info checklist should also be expanded slightly. Beyond what is already captured, the revised pack should explicitly ask for **Persada numbering format**, whether thermal receipts are needed for all companies or only selected ones, whether Official Receipt numbering is separate from invoice numbering in every company, whether Delivery Order can be issued without prices by default, whether quotations must always carry validity dates, and whether “prepared by / attention / project reference / PO reference” are mandatory fields or optional per template family. fileciteturn0file2 fileciteturn0file3 fileciteturn0file4

The risk register should gain a few more items as well: render-time remote asset fetching, webhook replay, profile edits changing structured issued-data views, database/media backup inconsistency, disk exhaustion from artwork uploads, and AI-provider retention/privacy mismatch. Those are not theoretical extras; they are natural extensions of the risks already recognized in the crosscheck. fileciteturn0file1 fileciteturn0file2

## Final revised recommendations for Codex

**E. Final revised recommendations to feed back into Codex:**

- Keep the seven locked decisions **verbatim** and repeat them in the revised output’s executive summary and implementation gate sections. fileciteturn0file1 fileciteturn0file2
- State plainly that the plan is **approved for revised audit output only**, and **blocked for scaffolding** until Phase 0.5 closes. fileciteturn0file0 fileciteturn0file1
- Add a **canonical source allowlist** for every company and document type, and explicitly classify all mixed workbook sheets as reference-only or denied-for-seeding. fileciteturn0file4 fileciteturn0file5
- Add a configurable **numbering policy model** per `company_id + document_type`, and treat final numbering format for **Wehdah, Nas Ceria, and Persada** as a freeze item before build. fileciteturn0file2 fileciteturn0file3 fileciteturn0file5
- Rename the internal `receipt` document type to **`official_receipt`**, and separate it from 60 mm quick-print behavior. fileciteturn0file2
- Promote `idempotency_keys`, `document_status_history`, `payment_allocations`, and `pdf_renders` into the revised schema/API guidance, while keeping them explicitly marked as **proposed** rather than final migrations. fileciteturn0file1
- Require issue-time structured snapshots for supplier, buyer, bank, terms, tax, currency/FX, and template version. fileciteturn0file1 fileciteturn0file2
- Make Wehdah artwork order **template-controlled by default**: invoice first, artwork after. fileciteturn0file4 fileciteturn0file5
- Expand acceptance criteria to include concurrency, year rollover, visual regression, unauthorized Telegram rejection, signed URL expiry, path traversal rejection, and backup restore drills. fileciteturn0file1 fileciteturn0file2
- Keep future MyInvois-related document types in a **future-only** section, not in v1 UI or workflow scope. fileciteturn0file1 fileciteturn0file2

**F. Locked decisions that were almost violated:**

The adjusted plan does **not directly break** your locked decisions, but a few areas come close and should be tightened. The closest near-misses are: the `number preview` endpoint, which can blur the “official number only on issue” rule; the mention of `refund_note` and self-billed variants, which can blur the “e-Invoice metadata-ready only” scope; mixed workbook sheets and naïve PDF deduplication, which can blur company isolation; and any loose “document revisions” wording, which can blur immutability for issued documents. These are fixable, but they should be called out explicitly in the next JSON/markdown revision so no downstream agent accidentally crosses the line. fileciteturn0file1 fileciteturn0file2 fileciteturn0file4 fileciteturn0file5